---
scope: Remediacion de la capa de dominio/infraestructura de expense-manager-1.0 antes de introducir backend real
purpose: build-substrate
altitude: epic
status: complete
updated: 2026-08-16T12:25
---

- (constraint) Working mode: Fast path (user chose speed over coaching); purpose: build substrate for Amelia (dev agent) to implement remediation
- (constraint) Bug confirmed by direct code read: usecases (create-transaction.ts:1, get-transactions.ts:1, get-balance.ts, get-monthly-stats.ts) import type TransactionRepository from @/domain/repositories barrel, but only ITransactionRepository is exported (transaction.repository.ts:3) -> tsc fails
- (constraint) Write path bypasses usecases: transaction-form.tsx and app/transactions/page.tsx import transactionRepository (concrete singleton) directly from @/infrastructure/repositories; CreateTransactionUseCase is dead code
- (constraint) use-transactions.ts hook instantiates usecases inline with useMemo(() => new GetXUseCase(transactionRepository), []) importing the concrete repository singleton directly -- read path also not going through a resolvable boundary
- (constraint) Domain entities (transaction.ts, budget.ts, category.ts, account.ts) are framework-clean, no React/Next imports -- this invariant already holds and must be preserved
- (constraint) Budget entity (id, categoryId, amount, month, year, spent, remaining, percentageUsed) defined in domain/entities/budget.ts but never referenced by any repository/usecase/component; categories/page.tsx recomputes isNearLimit/isOverBudget inline from a plain category.budget number field instead
- (constraint) formatCurrency (Intl.NumberFormat es-PE) duplicated identically in dashboard-stats.tsx, monthly-chart.tsx, category-chart.tsx -- Rule of Three already crossed, extraction warranted
- (constraint) Two competing design-token sources coexist: presentation/styles/theme.css (orphaned, zero imports found) vs app/globals.css shadcn radix-nova tokens actually in use; components also hardcode raw Tailwind color utilities (bg-emerald-500 etc.) bypassing both
- (constraint) No app/api/ routes, no .env config, no zod or any validation lib in package.json, no auth scaffolding, zero test files anywhere under src/
- (constraint) IDs generated via x- (mock-transaction.repository.ts:33) and similarly in mock-category.repository.ts -- collision-prone, not UUID-based
- (version) Stack confirmed from package.json: next 16.2.2, react/react-dom 19.2.4, typescript ^5, tailwindcss ^4, recharts ^3.8.1, framer-motion ^12.38.0, date-fns ^4.1.0, zod NOT installed
- (version) zod current stable is 4.4.3 (verified via web search, zod.dev release notes, Aug 2026) -- to be added as the validation library
- (decision) AD-1 [ADOPTED, tightened]: Layered/Clean Architecture dependency direction (presentation -> usecases -> repository interfaces -> infrastructure impls) becomes binding for READS AND WRITES, not just reads. Prevents: any presentation file importing @/infrastructure/* directly.
- (decision) AD-2 [ASSUMPTION]: repositories are only ever instantiated/imported inside server-only code (Next.js Server Actions under src/app/**/actions.ts). 'use client' components/hooks never import @/infrastructure/*; they call server actions and receive plain data. Chosen over a client-side DI/Context provider because it also closes the future backend-auth/secrets boundary for free and is the idiomatic Next.js 16 App Router pattern (boring technology).
- (decision) AD-3 [ASSUMPTION]: single repository factory (infrastructure/repositories/factory.ts) resolves ITransactionRepository/ICategoryRepository by a DATA_SOURCE env var (mock|api, default mock). Only server actions call the factory. This is the sole swap point for introducing the real backend later.
- (decision) AD-4 [ASSUMPTION]: every server action validates its input with a zod schema colocated in the same actions.ts file before calling a usecase; usecases/domain never receive unvalidated primitives from the network/form boundary.
- (decision) AD-5: derived/aggregate state (budget isNearLimit/isOverBudget, monthly trend deltas, balance) is computed exclusively inside usecases (GetMonthlyStatsUseCase, new GetBudgetStatusUseCase), never inline in a page/component. Prevents the categories/page.tsx-style duplication and the hardcoded fake trend percentages in dashboard-stats.tsx.
- (decision) AD-6 [ASSUMPTION]: cross-cutting utilities (formatCurrency, date helpers) live only under src/lib/ (the pre-existing shadcn convention), not a parallel shared/ tree from the original plan doc. Rationale: avoid two competing shared homes; Rule of Three already broken by 3 duplicate formatCurrency copies.
- (decision) AD-7: entity IDs are generated via crypto.randomUUID() in every repository implementation (mock and future real); Date.now()-based ids are a defect, not a convention, and must not be carried into any new repository.
- (decision) AD-8: a single design-token source of truth is app/globals.css; presentation/styles/theme.css (orphaned, zero imports) is deleted; components must consume semantic Tailwind tokens, not raw palette utilities like bg-emerald-500 hardcoded per-component.
- (decision) Server actions return a discriminated union { success: true, data } | { success: false, error: string } instead of throwing, so client components handle failures uniformly without ad hoc try/catch-and-toast per call site (folded into AD-2's boundary rule).
- (question) Open: real backend target (own API route handlers + DB vs external SaaS/BaaS) still undecided -- out of scope for this remediation spine, deferred to the backend architecture run that follows
- (direction) User (John) explicitly requested the architect (Winston) design the remediation plan for domain/infrastructure before backend work proceeds, after Mary's BA review flagged the audit findings
- (event) Reviewer gate: lint_spine.py clean (0 findings); version-reality reviewer verdict = stack/versions/codebase claims verified, but found dead source citation + missed tsc bug in domain layer; adversarial reviewer verdict = AD-2/AD-3 instantiation contradiction, factory signature gap, undecided Budget ownership (critical), plus UUID-assignment-layer and error-contract gaps (high)
- (decision) Applied fixes: corrected sources: path to expense-manager-1.0/docs/...; added explicit blocking-bug callout to AD-1 (ITransactionRepository import fix is step zero); rewrote AD-2/AD-3 so factory.ts is the sole instantiation point (actions.ts never news a repo directly), added server-only enforcement, async factory signature, singleton-mock/fail-loud-DATA_SOURCE; AD-5 now explicitly activates Budget entity + IBudgetRepository instead of leaving budget ownership open; AD-7 now states id assignment happens inside repository.create(); AD-8 closed the bg-[#hex] arbitrary-value loophole; State & cross-cutting convention now defines ActionResult<T>, zod-issue aggregation, throw-vs-result split, and no-raw-exception-leak rule; structural seed updated with IBudgetRepository/mock-budget.repository.ts/lib/types.ts and infrastructure/data/ purpose comment; diagram redrawn with actions.ts -> factory.ts edge
- (event) Deferred without spine edit (low stakes, noted to user instead): mermaid diagram omitting src/lib edge; page.tsx vs presentation/components ownership split -- neither risks real two-unit divergence at this altitude
- (event) Re-ran lint_spine.py post-fix: 0 findings
- (event) spine finalized
